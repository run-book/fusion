{
  "template":    "typescript",
  "name":        "@fusionconfig/jsonata",
  "description": "the precise version of jsonata we use: implements our abstraction",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}",
        "jsonata":                "2.0.4"
    },
    "devDependencies": {
    }
  },
  "links":       [
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    false
  }
}