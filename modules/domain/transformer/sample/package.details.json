{
  "template":    "typescript",
  "name":        "@fusionconfig/sample",
  "description": "These are samples of the data for a schema",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}"
    },
    "devDependencies": {
    }
  },
  "links":       [
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    false
  }
}