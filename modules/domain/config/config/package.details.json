{
  "template":    "typescript",
  "name":        "@fusionconfig/config",
  "description": "the interfaces for the configuration",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@laoban/fileops":        "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}"
    },
    "devDependencies": {
      "@itsmworkbench/jsyaml": "${properties.itsmVersion}"
    }
  },
  "links":       [
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    true
  }
}