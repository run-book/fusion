{
  "template":    "typescript",
  "name":        "@fusionconfig/browsertests",
  "description": "accessing the tests from the browser",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}"
    },
    "devDependencies": {
    }
  },
  "links":       [
    "@fusionconfig/config",
    "@fusionconfig/tests"
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    false
  }
}