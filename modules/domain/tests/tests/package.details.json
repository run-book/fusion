{
  "template":    "typescript",
  "name":        "@fusionconfig/tests",
  "description": "the interface for the tests",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}"
    },
    "devDependencies": {
    }
  },
  "links":       [
    "@fusionconfig/config"
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    false
  }
}