{
  "template":    "typescript",
  "name":        "@fusionconfig/services",
  "description": "How we process services in the config",
  "packageJson": {
    "dependencies":    {
      "@laoban/utils":          "${properties.laobanVersion}",
      "@laoban/fileops":        "${properties.laobanVersion}",
      "@itsmworkbench/urlstore": "${properties.itsmVersion}"
    },
    "devDependencies": {
      "@itsmworkbench/jsyaml": "${properties.itsmVersion}"
    }
  },
  "links":       [
    "@fusionconfig/config"
  ],
  "guards":      {
    "compile": true,
    "publish": true,
    "test":    true
  }
}